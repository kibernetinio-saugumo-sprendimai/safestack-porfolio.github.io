name: Pages CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Reject active runtime code and insecure links
        run: |
          ! grep -Eiq '<script|api\.github\.com|http://' index.html
          grep -Fq 'Content-Security-Policy' index.html
